export interface interfaceWord {
    id: number;
    name: string;
    desc: string;
    state: boolean;
}

export interface interfaceBoard {
    id: number;
    stateLetter: string;
    letterCorrect: string;
    letterGuess: string;
}
